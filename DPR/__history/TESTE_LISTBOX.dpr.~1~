program TESTE_LISTBOX;

uses
  Vcl.Forms,
  uInicio in '..\PAS\uInicio.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
